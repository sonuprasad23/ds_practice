# List all files and directories in the current directory using os.listdir('.').
import os
print(f"Contents of '.': {os.listdir('.')}") # '.' means current directory
# print(f"Contents of '..': {os.listdir('..')}") # Parent directory