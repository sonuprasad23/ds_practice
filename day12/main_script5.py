import geometry 
c_area = geometry.circle.area(10)
r_area = geometry.rectangle.area(3, 7)

print(f"Circle (radius 10) Area (via geometry): {c_area:.2f}")
print(f"Rectangle (3x7) Area (via geometry): {r_area}")